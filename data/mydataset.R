# ### List of datasets
# tamoxifen = readRDS(system.file("extdata", "tamoxifen.Rds", package = "power4peaks"))
#
# # Generate data files
# usethis::use_data(tamoxifen,
#                   overwrite = T)
